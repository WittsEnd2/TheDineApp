var myFirebaseRef = new Firebase("https://sweltering-inferno-9361.firebaseio.com/Users");
var newPostRef = myFirebaseRef.push();

  newPostRef.set({
      username: 'test',
      password: 'testing'
  });
  myFirebaseRef.on('child_added', function(snapshot, prevChildKey){
    var newPost = snapshot.val();
    console.log("Username: " + newPost.Users.username);
    console.log("Password: " + newPost.Users.password);
  });
